<div class="show-wedding">
<h2><%= @wedding.title %></h2>

<h3>Wedding Date: <%= @wedding.wedding_date.strftime('%d . %B . %Y') %></h3>

<%= link_to "Edit My Wedding", edit_wedding_path(@wedding), class: "edit-my-wedding" %>
<%= link_to "Delete My Wedding", wedding_path, method: :delete,  class: "edit-my-wedding" %>

<h3>My Role: <%= @wedding.roles.map{|x| x.title}.join(", ") %></h3>

<h3>Role Description: <%= @wedding.roles.map{|x| x.role_description}.join(", ") %></h3>

<div>